// import { getElement } from '../helpers';

// describe('getElement', () => {
//   it('returns an element', () => {
//     const selector = '.btn-add';
//     const element = `<button class="selector">`;

//     expect(element).toEqual(getElement(selector));
//   })
// })